version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unconvert
    - wastedassign

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
